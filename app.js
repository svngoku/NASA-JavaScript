const loadash = require('lodash')
const nasa = 'NASA';

// Multiplication des valeurs par 3 avec la methode map de lodash
lodash.map([1,2,3], (n)) => { return n * 3});
// 3 , 6 , 9
console.log("We love " + nasa)
